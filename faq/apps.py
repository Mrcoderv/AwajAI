from django.apps import AppConfig


class FaqConfig(AppConfig):
    """Application configuration for the FAQ knowledge base."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "faq"
